const myArr = [0,1,3,3,5,6]
/*js arrays are resizable and can contain mix of data type
//js array-copy-operation create shallow copies, shallow copy of an object is a copy whose property share the same reference,
and whatever changes we make which changes in original arrays too
//Deep copy of an object is a copy whose property doesn't share the reference 
*/
const studentName = ["Ritz", "Adam", "Rachel"]

const myArr2 = new Array(1,2,4,5,6)
console.log(myArr[0]);